## Contribution Rules

- One package per directory
- Directory name MUST match pkgname
- `template` file is mandatory
- No binary files
- No generated files

## Categories

- core: system, kernel, toolchain
- extra: desktop, apps
- multilib: 32-bit only

All submissions are validated automatically.
